@model IEnumerable<MadeiraDeCedro.Domain.Entities.Servicos>

<h1>Serviços</h1>
<div class="w-100 card border-0 p-4">
    <div class="card-header bg-success bg-gradient m-lg-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">Lista de Serviços</h2>
            </div>
        </div>
    </div>
    </div>
<div class="card-body border-bottom p-4">
    <div class="row pb-3">
        <div class="col-6 offset-6 text-end">
                                                @*Col bootstrap para dividir o conteudo segundo o sistema de linhas e grelhas,
                                                cada grelha tem 12 colunas, podem ser divididas em diferentes quantidades cuja soma das
                                                colunas dá sempre 12. Text-end controla a posição do botão  *@
            <a asp-controller="Servicos" asp-action="Create" class="btn btn-secondary">
                <i class="bi bi-plus-circle"></i> Criar Servico
            </a>
        </div>
    </div>
    </div>
<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>Nome</th>
            <th>Descrição</th>
            <th>Madeira</th>
            <th>Movel</th>
        </tr>

    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td>@obj.Nome</td>
                <td>@obj.Descrição</td>
                <td>@obj.Madeira</td>
                <td>@obj.Movel</td>
                <td>
                <div class="w-75 btn-group role=group">
                    <a asp-controller="Servicos" asp-action="Update" asp-route-ServicoId="@obj.Id" class="btn btn-success mx-2">
                        <i class="bi bi-pencil-square"></i> Edit
                    </a>

                    <div class="w-75 btn-group role=group">
                            <a asp-controller="Servicos" asp-action="Delete" asp-route-ServicoId="@obj.Id" class="btn btn-danger mx-2">
                            <i class="bi bi-trash-fill"></i> Delete
                        </a>
                        </div>
                    </div>
                    </td>

            </tr>

        }
    </tbody>
</table>